language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-anication' ]; then cd .. && eval "mv $currentfolder generator-anication" && cd generator-anication; fi

